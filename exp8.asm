PRINT MACRO M
	MOV DX,OFFSET M
	MOV AH,09H
	INT 21H
ENDM
ASSUME CS:CODE,DS:DATA
DATA SEGMENT
	STR1 DB 80 DUP('$')
	STR2 DB 80 DUP('$')
	MS1 DB 'ENTER STRING1',0AH,0DH,'$'
	MS2 DB 'ENTER STRING2',0AH,0DH,'$'
	MS3 DB 'CONCATENATED STRING',0AH,0DH,'$'
DATA ENDS
CODE SEGMENT
	START:
	MOV AX,DATA
	MOV DS,AX
	MOV CL,0H
	PRINT MS1
	MOV SI,OFFSET STR1
LA1: 	MOV AH,01H
	INT 21H
	CMP AL,0DH
	JZ LA2
	MOV [SI],AL
	INC CL
	INC SI
	JMP LA1
LA2: 	PRINT MS2
        MOV BL,CL
	MOV DI,OFFSET STR2
LA3:	MOV AH,01H
	INT 21H
	CMP AL,0DH
	JZ  LA7
	MOV [DI],AL
	INC CL
	INC DI
	JMP LA3

LA7:	MOV DI,OFFSET STR2
	
LA4:	
	MOV AL,[DI]
	MOV [SI],AL
        INC SI
	INC DI
	DEC CL
	JZ LA5
	JMP LA4
	
LA5:	PRINT STR1
	MOV AH,4CH
	INT 21H
CODE ENDS
END START
	
